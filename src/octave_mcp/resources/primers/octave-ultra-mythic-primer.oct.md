===ULTRA_MYTHIC_PRIMER===
META:
  TYPE::PRIMER
  VERSION::"7.0.2"
  TOKENS::"448"
  COMPRESSION_TIER::ULTRA
  LOSS_PROFILE::"[preserve:NEVER_constraints∧delegation,drop:rationale]"
§1::ESSENCE
PURPOSE::"Crush identity prose→mythic atoms at 60%, soul⊕constraints intact"
OCTAVE::"Olympian Common Text And Vocabulary Engine — Semantic DSL for LLMs"
METHOD::[role_verbatim→archetype_atom→NEVER_atoms→delegation]
§2::MAP
EXAMPLES:
  ```
  role         → ROLE verbatim
  archetype    → NAME<facet>              ATLAS<structure>
  must_never   → NEVER[ATOM,ATOM]         one constructor, never NEVER::atom
  delegation   → FROM→TO                  DELEGATE[IMPL_LEAD]
  mission      → ATOM⊕ATOM
  principle    → key phrase only          CONSTRAINT_CATALYSIS
  pick_myth    → ZEUS∨ARES∨ATLAS∨HERMES   by domain; unknown → nearest atom
  ```
§3::SYNTAX
OPERATORS:
  ```
  ::       binds
  →        flow / delegates to
  ⊕        synthesis
  ⇌        tension
  ∧        conjunction / all-required
  ∨        disjunction / alternative
  <>       archetype facet
  []       args on an operation
  NEVER[]  constraint
  ```
§4::ONE_SHOT
IN::"Architect designs, never implements; delegates to the implementation lead"
OUT:
  ```
  ID::ARCHITECT<ATLAS>
  CONSTRAINT::NEVER[DIRECT_IMPL]→DELEGATE[IMPL_LEAD]
  ```
§5::VALIDATE
MUST::[
  valid_OCTAVE,
  "preserve_§_names_verbatim",
  every_source_NEVER_kept,
  delegation_intact,
  archetype_in_angle_brackets,
  do_not_copy_example_role
]
===END===
